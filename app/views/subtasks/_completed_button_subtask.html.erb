<%= form_with(model: [@list, subtask]) do |form| %>
    <%= form.check_box :completed, checked: true, class: "visually-hidden" %>
    <%= form.submit "Complete", class: "btn btn-primary", class: "btn btn-outline-secondary " %>
<% end %>